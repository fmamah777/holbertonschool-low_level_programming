#include <stdio.h>
#include "main.h"
/* this is a betty styled this format in c coding */
/**
 *main - is lower function as part of putchar
 *Return: zero or one
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
