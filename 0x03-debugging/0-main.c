#include "main.h"

/**
  * main - prints if integer is positive or negative
  * Return: 0
  */

int main(void)
{
	int i;

	// checked the function using 0 instead of 98
	// changed i = 98; => i = 0;
	i = 0;
	positive_or_negative(i);

	return (0);
}
