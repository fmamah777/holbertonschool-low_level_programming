#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string.
 * @s: this is the set variable
 * Return: 0
 */
int _strlen(char *s);
{
	int car;

	for (car = 0; s[car] != '\0'; car++)
	{
		continue;
	}
		return (car);
}
