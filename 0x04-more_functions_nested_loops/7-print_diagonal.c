#include "main.h"
#include <stdio.h>

/**
 * print_line - this prints a straight line in the terminal.
 * @n: n is a parameter
 * Return(void)
 */

void print_line(int n)
{
	int z;

	for (z = 0; z < n; z++)
	{
		_putchar('\');
	}
		_putchar('\n');
}
