#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies numbers.
 * @a: is multiplied against b
 * @b: is multiplied against a
 * Return: the product of whatever a and b is.
 *
 */

int mul(int a, int b)
{
	return (a * b);
}
