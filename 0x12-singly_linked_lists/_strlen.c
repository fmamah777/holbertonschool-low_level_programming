#include "lists.h"

/**
* _strlen - function being usedn
* @s: first parameter
* Return: Always 0
*/

int _strlen(char *s)
{
	int strlength;

	for (strlength = 0; s[strlength] != '\0'; strlength++)
	{
		continue;
	}
	return (strlength);
}
