#include "main.h"
#include <stdio.h>

/**
 * _abs - checks abs value
 * @n: N the exact number value
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	else if ( n < 0)
	{
	return (-n);
	}
return (0);
}
